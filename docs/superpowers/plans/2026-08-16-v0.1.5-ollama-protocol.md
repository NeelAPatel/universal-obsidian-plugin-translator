# UOPT v0.1.5 implementation plan

1. Add failing provider tests for Ollama line protocol parsing, partial recovery, unresolved-only retry, and unchanged OpenAI behavior.
2. Implement a dedicated Ollama line-protocol prompt/parser and switch Ollama chat calls away from JSON-schema `format`; keep `stream:false`, `think:false`, and use temperature 0.
3. Add failing translator tests for source-module-aware batching and partial translation propagation on terminal provider failure.
4. Implement semantic grouping by nearest `// src/...` module marker while retaining character-budget splitting and source order.
5. Add failing service test proving recovered partial translations are persisted to translation memory while the source file remains untouched after terminal protocol failure.
6. Implement partial-memory persistence in the service catch path.
7. Update diagnostics matching, version metadata, changelog, README, and release tests for 0.1.5.
8. Run the full suite, build, runtime verification, syntax checks, and release packaging.
9. Push branch, open PR, verify CI, merge to main, verify main CI + release, compare release asset hashes, and produce install ZIP.
